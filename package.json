{
  "name": "gestao-condominio-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo para Gestão de Condomínio com separação Frontend/Backend",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm start -w frontend",
    "lint": "npm run lint -w frontend",
    "dev:all": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "install:all": "npm install && npm install -w frontend && npm install -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}